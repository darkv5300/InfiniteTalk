build:
  gpu: true
  cuda: "11.8"
  python_version: "3.10"
  python_requirements: requirements.txt   # 👈 บอกตรงนี้ให้ใช้ไฟล์ requirements.txt

predict: "predict.py:Predictor"
